# Objective - create a number guessing game
#
# Requirements:
#   The program chooses a random number between 1 and 100
#   The user tries a number
#   If the number is incorrect, the program gives a hint -
#       if the user's number is greater or smaller than the correct number.
#   If the number is correct, the program tells the user how many steps
#       they used to guess the number.

from random import randint
