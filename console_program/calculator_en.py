# Requirements:
#   Accept 2 numbers and an operator from the user
#   The operator must be one of the following: +, -, *, /
#   Depending on the operator, the program must add, subtract, multiply or divide the 2 numbers
#   Print the result to the console
#   If the user types an unknown operator, print a message about it and exit
